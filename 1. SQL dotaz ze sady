-- Sada SQL Dotazů
-- Výzkumné otázky


-- 1) Rostou v průběhu let mzdy ve všech odvětvích,
-- nebo v některých klesají?



SELECT 
	cpib.name AS "odvětví",
	cpay.value AS "mzdy",
	cpay.payroll_year AS "průběžné roky",
	cpay.payroll_quarter AS "kvartál"
FROM czechia_payroll cpay
JOIN czechia_payroll_industry_branch cpib
	ON cpay.industry_branch_code = cpib.code
WHERE cpay.value_type_code = 5958
	AND cpay.payroll_quarter = 4;

-- Podle sledování kvartálů to vypadá,
-- že ve 4. kvartálu se mzda vždy liší a to tím rozdílem, 
-- že je vyšší než ty ostatní první tři. 

-- Po analyzování všech dat z tohoto dotazu vychází následující odpověď:
-- Zvyšování se v různých odvětvích mírně liší a to zvedáním, či snižováním mezd.
-- Z většiny oborů bylo patrné, že se postupem roků mzda vždy zvyšuje ve 4. kvartálu.
-- Celkové shrnutí je, že v minimálních případech a kvartálech se mzda kolísavým 
-- způsobem snižuje. Ve většině případů se mzda zvyšuje. 
